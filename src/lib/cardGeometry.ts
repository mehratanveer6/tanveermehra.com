// Aspect ratio of the actual exported card artwork (front.png), not a
// generic business-card ratio — the artwork is the production asset, so its
// own proportions are the source of truth for every layout calculation.
export const CARD_ASPECT_RATIO = 755 / 427;
